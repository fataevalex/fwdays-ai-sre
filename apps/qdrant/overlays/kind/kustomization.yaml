apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Qdrant on kind is installed directly via Helm (see kind/helm-values/qdrant.yaml).
# This overlay is a placeholder for any additional resources if needed.
resources: []
