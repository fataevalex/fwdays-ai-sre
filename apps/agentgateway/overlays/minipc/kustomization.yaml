apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# minipc overlay: deploy only Gateway config resources.
# agentgateway Helm chart is managed by Flux (gitea-runner-helm repo).

resources:
  - gateway.yaml
  - llm-backend.yaml
