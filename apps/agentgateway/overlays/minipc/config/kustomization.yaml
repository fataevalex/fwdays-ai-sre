apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gateway.yaml
  - llm-backend.yaml
  - admin-nginx-configmap.yaml
  - oauth2-proxy.yaml
  - admin-ingress.yaml
  - tracing-policy.yaml
  - tracing-reference-grant.yaml

  - proxy-configmap-patch.yaml
