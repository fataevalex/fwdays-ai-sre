apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nginx-configmap.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: kagent-ui
      namespace: kagent
