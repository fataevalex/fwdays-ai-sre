apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# kind overlay: plain Kubernetes resources only (no ArgoCD Applications).
# Deployed directly via Helm in the Makefile.

resources:
  - ../../base/namespace.yaml
  - ../../base/nginx-configmap.yaml
  - ../../base/kustomize-patch/deployment-patch.yaml
