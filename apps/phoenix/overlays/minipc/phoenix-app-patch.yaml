apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: phoenix
  namespace: argocd
spec:
  source:
    helm:
      valuesObject:
        nodeSelector:
          node-pool: minipc
        service:
          type: ClusterIP
          port: 6006
