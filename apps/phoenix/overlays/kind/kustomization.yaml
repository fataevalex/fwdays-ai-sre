apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Phoenix on kind is installed directly via Helm (see kind/helm-values/phoenix.yaml).
# This overlay is a placeholder for any additional resources if needed.
resources: []
